제목 출력
룰 설명
1에서 9사이 수 중 3개 랜덤 선택
life = 10

while True:
    생명 출력
    guess = 플레이어가 예측한 정답 입력
    strike, ball = 0
    for 3 times:
        if guess[i] == answer[i]:
            strike
        elif guess[i] in answer:
            ball
    strike, ball 출력
    if strike == 3:
        정답 출력 , break
    생명 감소
    if life == 0:
        게임오버, break
